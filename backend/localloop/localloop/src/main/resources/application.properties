# Server
server.port=8080

# Database (Supabase)
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?user=postgres.xvvvrzvrurdsefpfmkwm&password=Feb-09187664254
spring.datasource.username=postgres
spring.datasource.password=Feb-09187664254
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=localloop_super_secret_key_for_jwt_authentication_2026_abelgas
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Google OAuth2 (add later)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile

# Mail (add later)
# spring.mail.host=smtp.mailtrap.io
# spring.mail.port=587
# spring.mail.username=YOUR_MAILTRAP_USERNAME
# spring.mail.password=YOUR_MAILTRAP_PASSWORD
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

