    # Server
    server.port=8080

    # Database (Supabase)
    spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?user=postgres.xvvvrzvrurdsefpfmkwm&password=Feb-09187664254
    spring.datasource.username=postgres
    spring.datasource.password=Feb-09187664254
    spring.datasource.driver-class-name=org.postgresql.Driver

    # JPA
    spring.jpa.hibernate.ddl-auto=validate
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

    # JWT
    app.jwt.secret=localloop_super_secret_key_for_jwt_authentication_2026_abelgas
    app.jwt.expiration=86400000

    # CORS
    app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

    # Google OAuth2
    spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
    spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
    spring.security.oauth2.client.registration.google.scope=email,profile

    # Mobile OAuth (Android client ID from Google Cloud Console)
    app.google.android-client-id=${GOOGLE_ANDROID_CLIENT_ID}

    # Mail (Gmail SMTP)
    spring.mail.host=smtp.gmail.com
    spring.mail.port=465
    spring.mail.username=abelgasfrancis@gmail.com
    spring.mail.password=${MAIL_PASSWORD}
    spring.mail.properties.mail.smtp.auth=true
    spring.mail.properties.mail.smtp.ssl.enable=true
    spring.mail.properties.mail.smtp.starttls.enable=false
    spring.mail.properties.mail.smtp.starttls.required=false
    spring.mail.properties.mail.smtp.connectiontimeout=5000
    spring.mail.properties.mail.smtp.timeout=5000
    spring.mail.properties.mail.smtp.writetimeout=5000

    # Mail sender display name
    app.mail.from=abelgasfrancis@gmail.com
    app.mail.from-name=LocalLoop

    weather.api.key=${WEATHER_API_KEY:}

    spring.servlet.multipart.max-file-size=5MB
    spring.servlet.multipart.max-request-size=5MB
    spring.session.store-type=none
    spring.devtools.restart.additional-exclude=**/.sessions
    server.servlet.session.persistent=false
    spring.cache.type=simple
